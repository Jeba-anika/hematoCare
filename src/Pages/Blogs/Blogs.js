import React from 'react';
import {TitleChange} from '../../Title/ChangeTitle'


const Blogs = () => {
    return (
        <div>
            {TitleChange('Blogs')}
            <h2>Blogs</h2>
        </div>
    );
};

export default Blogs;